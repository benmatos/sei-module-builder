<?php /* View: listar {{ tabela.nome }} */ ?>
<h2>Listagem — {{ tabela.nome }}</h2>
<table class="infraTable">
  <thead>
    <tr>
      {% for col in tabela.colunas %}
      <th>{{ col.nome | pascalcase }}</th>
      {% endfor %}
      <th>Ações</th>
    </tr>
  </thead>
  <tbody>
    <?php foreach ($arr{{ tnome }} as $row): ?>
    <tr>
      {% for col in tabela.colunas %}
      <td><?php echo htmlspecialchars($row['{{ col.nome | pascalcase }}'] ?? ''); ?></td>
      {% endfor %}
      <td><a href="?acao=alterar&id=<?php echo $row['Id']; ?>">Editar</a></td>
    </tr>
    <?php endforeach; ?>
  </tbody>
</table>
<a href="?acao=cadastrar">Novo</a>
