<?php
// Controller: {{ tabela.nome }} — Gerado por SEI Module Builder

class {{ ns }}{{ tnome }}Controller extends InfraController {

    public function listar() {
        $obj{{ tnome }}DTO = new {{ ns }}{{ tnome }}DTO();
        $obj{{ tnome }}RN  = new {{ ns }}{{ tnome }}RN();
        $obj{{ tnome }}RN->listar($obj{{ tnome }}DTO);
        InfraHtml::exibirTemplate(
            '{{ d.slug }}/{{ d.slug }}_{{ tabela.nome }}_listar',
            array('arr{{ tnome }}' => $obj{{ tnome }}DTO->toArray())
        );
    }

    public function cadastrar() {
        $obj{{ tnome }}DTO = new {{ ns }}{{ tnome }}DTO();
        if (InfraHtml::isPostBack()) {
            // Preencher DTO a partir do POST
            $obj{{ tnome }}RN = new {{ ns }}{{ tnome }}RN();
            $obj{{ tnome }}RN->cadastrar($obj{{ tnome }}DTO);
            InfraHtml::exibirMensagem(InfraHtml::MSG_SUCESSO, 'Registro cadastrado com sucesso.');
            return;
        }
        InfraHtml::exibirTemplate(
            '{{ d.slug }}/{{ d.slug }}_{{ tabela.nome }}_cadastrar',
            array('obj{{ tnome }}' => $obj{{ tnome }}DTO)
        );
    }
}
